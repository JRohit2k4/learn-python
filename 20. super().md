## super() :
- Function used in a child class to call methods from a parent class (superclass).  
- Allows you to extend the functionality of the inherited methods.

```python

class Attribute: #super-class
      def __init__(self, color, is_filled):
          self.color = color
          self.is_filled = is_filled

      def describe(self):
           print(f"{self.shape} is {self.color} and {filled if self.is_filled else 'not filled'}") #used list comprehension


class Circle(Attribute):
      def __init__(self, shape, color, is_filled, radius):
          super().__init__(color, is_filled) #super function called
          self.shape = shape
          self.radius = radius

class Square(Attribute):
      def __init__(self, shape, color, is_filled, width):
          super().__init__(color, is_filled) #super function called
          self.shape = shape
          self.width = width

class Triangle(Attribute):
      def __init__(self, shape, color, is_filled, width, height):
          super().__init__(color, is_filled) #super function called
          self.shape = shape
          self.width = width
          self.height = height

#giving the values in parameters
circle = Circle("circle", "blue", False, 6)
square = Square("square", "green", True, 3)
triangle = Triangle("triangle","red", True, 5, 5)

print(f"Color of Circle is: {circle.color}")
print(f"Is square filled with color: {square.is_filled}")
print(f"Height of Triangle is: {triangle.height}")

circle.describe()

```
